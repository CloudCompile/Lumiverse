{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-instance",
  "description": "Allows only the hidden local tray host to manage a remote instance without exposing OAuth credentials to any WebView it loads.",
  "windows": ["main"],
  "permissions": ["remote-instance-commands"]
}
